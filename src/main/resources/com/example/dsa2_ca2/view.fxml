<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.control.MenuItem?>
<?import javafx.scene.control.MenuBar?>
<?import javafx.scene.control.ToggleGroup?>
<?import javafx.scene.control.Menu?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.canvas.Canvas?>

<BorderPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml" fx:controller="com.example.dsa2_ca2.controller.Controller">
    <top>
        <VBox alignment="CENTER" fx:id="topBox">
            <MenuBar>
                <fx:define>
                    <ToggleGroup fx:id="viewToggleGroup"/>
                </fx:define>

                <Menu text="File">
                    <MenuItem text="Exit" onAction="#onExit"/>
                </Menu>

                <Menu text="Setup">
                    <MenuItem text="Choose Points" onAction="#choosePoints"/>
                    <MenuItem text="Add Room" onAction="#onAddRoom"/>
                    <MenuItem text="Add Exhibit" onAction="#onAddExhibit"/>
                    <MenuItem text="Add Edge" onAction="#onAddEdge"/>
                </Menu>

                <Menu text="Traversal">
                    <MenuItem text="BFS" onAction="#onBFS"/>
                    <MenuItem text="PixelBFS"  onAction="#onPixelBFS"/>
                    <MenuItem text="DFS" onAction="#onDFS"/>
                    <MenuItem text="Dijkstra" onAction="#onDijkstra"/>
                    <MenuItem text="Most Interesting Route" onAction="#onDijkstraInteresting"/>
                </Menu>

            </MenuBar>

        </VBox>
    </top>

    <center>
        <VBox fx:id="imageContainer" spacing="10" alignment="CENTER">
            <Canvas fx:id="mapCanvas" />
        </VBox>
    </center>

    <bottom>
        <HBox fx:id="bottomBox" spacing="10" alignment="CENTER">
        </HBox>
    </bottom>

</BorderPane>

